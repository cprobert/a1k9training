---
branch: chore/kiss-ssg-2.2.1
base: master
status: closed
opened: 2026-09-12
closed: 2026-09-12
---

# Session — 2026-09-12: Upgrade to kiss-ssg 2.2.1 and adopt aliases and {{link}}

## Intent (captured at kiss-branch-open)

**Objective:** Move the site from kiss-ssg 2.1.0 to 2.2.1 and take up the two
2.2.x features it has a use for — `aliases` (so kiss writes `_redirects` and
can report a page removed or moved without one) and `{{link}}` (so an internal
link is resolved by page id at render and a dead one fails the build) — with
the build output otherwise byte-identical.

**Success criteria:**

- [ ] `package.json` asks for `^2.2.1`, the lockfile resolves it, and
      `npx kiss-ssg check --summary generate.js` is `ok` with 19 pages,
      0 failed and no `broken link:` line (baseline at open: 19 pages, 0 failed,
      570 internal references, none broken).
- [ ] `src/assets/_redirects` is deleted. Its 14 rules now live as `aliases`:
      7 on the course records in `src/models/courses/*.json`, 2 on the
      consultation records in `src/models/behavioural-consultations/*.json`,
      4 on the about records in `src/models/about/*.json`, and `/find-us/` on
      the contact `.page()` in `generate.js`.
- [ ] The generated `docs/_redirects` carries exactly those 14 sources, each
      as one `<old> <canonical> 301` line, targets identical to the hand-written
      file's (`/courses/puppy-socialisation`, `/about/`, `/contact/`, …).
      `report().redirects` shows `aliases: 14`, `collisions: []`,
      `removed: []`, `moved: []`. The report's asset count drops from 92 to 91.
- [ ] No hardcoded internal page `href` remains in `src/pages`, `src/layouts`
      or `src/partials`; every one is `{{link "<id>" canonical=true}}` (ids per
      `AIKB/site-map.md`: `index`, `contact`, `courses/index`,
      `courses/course/<slug>`, `behavioural-consultations/index`,
      `behavioural-consultations/consultation/<slug>`, `about/index`,
      `about/<slug>`). Asset hrefs (`/fonts/…`, `/favicon.ico`) stay as they
      are — `{{link}}` is for pages.
- [ ] `canonical=true` is mandatory on every `{{link}}`: this site is not
      `extensionLess`, so a bare `{{link}}` renders `/courses/bronze-obedience.html`,
      which Netlify's pretty-URL rule would 301 back to the slashless form and
      `qa:preview`'s redirect-free check would fail. The check diff proves the
      swap was lossless: after the `{{link}}` step every page is `=` except
      those that carried a slashless section href (`/courses`, `/about`,
      `/behavioural-consultations` — 4 occurrences at open); those are `~` and
      their only change is the trailing slash. Nothing else in any page moves.
- [ ] The URLs `generate.js` builds by hand (`SECTIONS[].url`, `ladderStep`,
      the breadcrumb child `url`) either go through the same id → served-path
      resolution or are left as they are with a comment saying why — decided
      at the first pulse once it is known whether the `link` helper is callable
      from another helper. Either outcome is acceptable; silent drift between
      the two is not.
- [ ] The five notes the record asks for exist and are stamped with the
      subject hash from `report().aikb.subjects`:
      `AIKB/notes/controllers/{about,behavioural-consultations,course,faqmapper}.md`
      and `AIKB/notes/pipeline/tailwind.md`. The check prints no `note …:` line.
- [ ] `CLAUDE.md`'s Deploy paragraph names `^2.2.1` and what the site now
      relies on from it (aliases → `_redirects`, `{{link}}`, the record under
      `AIKB/`). The `_redirects` sentence under Assets and caching is rewritten:
      the file is generated from `aliases`, not kept in `src/assets`.
- [ ] `.claude/settings.json` enables both `kiss-ssg@kiss-ssg` and
      `kiss-memory@kiss-ssg` (already staged in the working tree at open).
- [ ] Eyeball: `npm run qa` passes (page count, no-Bootstrap, SEO, snapshot,
      compare, axe), and `/pr-verify` against the deploy preview reports every
      `_redirects` rule answering 301 to the same target as before.

**Non-goals / out of scope:**

- No `.feed()` — no page on this site carries a date.
- No switch to `extensionLess`, no change to any served URL, no copy or
  design change.
- No controller refactor beyond what a note needs to describe; no change to
  `qa/baseline/content.json` (the snapshot compare should still pass as is,
  because the page text does not change).
- Not fixing the CRLF/LF churn git warns about on every file; it is noise
  inherited from the working copy, not this branch's.

**Impact surface:** build script & config — the version bump and the contact
alias sit in `generate.js`, and the record under `AIKB/` opts the site into
findings every build. It also reaches **templates & partials** (every internal
href), **data & controllers** (aliases on fan-out records in `src/models`; the
four controller notes), and **assets & deploy** (`_redirects` leaves
`src/assets` and becomes build output; the pipeline note). Blast radius is
every page, which is why the byte-identical criterion carries the weight.

**Expected shape:** planned — the destination is known from the 2.2.0
changelog and the page-id table; the one open question (whether `generate.js`'s
hand-built URLs can share the resolver) is bounded and named above.

**Inherited feedback:** none — first session on this site.

**Inherited at open:**

- Five `note missing:` findings from the first record (the four controllers
  and the tailwind pipeline step). Nobody has written a note yet; they are
  taken on as a criterion above rather than left as inherited.
- Uncommitted before the branch was cut and carried onto it: the 2.2.1 bump in
  `package.json`/`package-lock.json`, the kiss-memory plugin enable in
  `.claude/settings.json`, and a line-endings-only touch on `CLAUDE.md`.
- No `check` diff at open: the record was made this session, so the baseline
  is the state at open by construction.

### Amendments

<!-- Dated notes where the remit legitimately expanded mid-branch. Good drift is
     recorded here and stays on this branch; a new branch is the operator's call. -->

- **2026-09-13, post-close, operator's call** — the QA compare baseline refresh, listed as a non-goal at open, is folded into this branch after the operator chose to merge green ("yes do a baseline refresh"). `qa/baseline/content.json` is re-snapshotted from a build of master (built in a throwaway worktree, so it is the last merged master, not this branch), and `qa/snapshot.mjs` now records internal links without a trailing slash so `/courses` and `/courses/` — one page on Netlify — cannot read as a lost link. Against that baseline this branch's compare is `ok` on all 20 pages. Lighthouse and axe baselines were not regenerated: nothing in the CI chain reads them. Also on this branch after close: the lockfile fix (6934bfc) for the two nested entries a Windows npm install pruned, which broke `npm ci` on Linux CI.

## Pulse log

<!-- Appended by kiss-branch-pulse, one dated line per checkpoint: criteria status,
     the evidence, and the decision. Append-only — the Intent above is immutable,
     and the criteria are ticked only at close. -->

- **2026-09-12** (slice 1: bump + `{{link}}`, commits f215efa, c14dc05) — criterion 1 met (`ok`, 19 pages, 0 failed, 513 internal references none broken; the drop from 570 is 3 section links × 19 pages deduplicating once navbar and footer agree on the slashed form). Criteria 4, 5 met: 65 hrefs and one form action across 12 files now go through `{{link … canonical=true}}`; a tree diff of `docs/` before and after, with only `href="/{courses,about,behavioural-consultations}"` normalised to the slashed form, is identical, sitemap `lastmod` aside. Criterion 6 resolved on the "share the resolver" side: `generate.js` gains `linkTo()` over `kiss.handlebars.helpers.link`, and `SECTIONS`, the breadcrumb trail and the course ladder resolve through it at render time. Criterion 9 met (settings committed). Criteria 2, 3, 7, 8, 10 not yet (aliases, notes, CLAUDE.md, qa). Check diff: all 19 pages `~`, `= 0` — the open's estimate that only pages carrying a slashless href would move was wrong, because the navbar is on every page; the proof of losslessness is the normalised tree diff, not the hash diff. No new note findings; the five inherited `note missing:` lines stand. Decision: continue — next slice is aliases.
- **2026-09-12** (slice 2: aliases, commit HEAD) — criteria 2, 3 met: `src/assets/_redirects` deleted; 13 records carry `aliases` and the contact `.page()` carries `/find-us/`; generated `docs/_redirects` has the same 14 from→to pairs as the old file (verified by sorted diff), `report().redirects` = `aliases: 14, collisions: [], removed: [], moved: []`, assets 92 → 91, `ok`, 513 links none broken. HTML byte-identical to slice 1's build. Unasked but adjacent, accepted: the four `next.link` URLs in `src/models/about/*.json` were hand-written paths the template slice could not see — now `next.id` rendered through `{{link}}`; `qa/preview.mjs` reads `docs/_redirects` and `.github/workflows/preview-qa.yml` gains an `npm run build` step so the file exists in CI (surface: assets & deploy, already declared). Check diff unchanged from slice 1 (19 `~`, all navbar slash). No new note findings. Decision: continue — next slice is the five notes, then CLAUDE.md, then `npm run qa`.
- **2026-09-12** (slices 3–4: notes, CLAUDE.md, QA; commits db9c2f7, 61d0a58) — criterion 7 met: five notes written and stamped, check prints no `note …:` line (four `dangling` findings on first write were bare filenames in my own notes, fixed before commit). Criterion 8 met: CLAUDE.md Deploy names `^2.2.1` and the three 2.2 features relied on; the `_redirects` sentence is rewritten; a `{{link}}` rule and a Knowledge-base section added. That commit also carried two changes the operator had staged meanwhile: the tailwind migration plan moved into `planning/sessions/` and a new intro paragraph — theirs, intended, left in. Criterion 10 partial: `npm run qa` passes page count, no-Bootstrap, SEO and snapshot; **compare fails, inherited** — `qa/baseline/content.json` was last refreshed for PR #23 and still lists `/find-us/`, so PR #24's rename, breadcrumbs, ladder and forms show as issues on every page and the gate fails on master too; refreshing the baseline is a non-goal here and belongs on master. axe, run standalone because the chain stopped at compare: 20 pages × 2 widths: critical 0, serious 4, moderate 4, minor 0. `/pr-verify` still to run once a PR exists. Decision: **ready to close** — every criterion this branch can meet is met; the compare failure is recorded as inherited for the close to carry, not tick.
- **2026-09-12** (axe detail, same pulse) — all 8 axe violations (4 serious: `document-title`, `html-has-lang`; 4 moderate: `landmark-one-main`, `page-has-heading-one`; each at 375 and 1440) are on `/googleab62e0ee5f306664`, the Google site-verification stub, which is a one-line token file and must stay as issued. Every real page scans clean at both widths. Identical to the 2026-09-09 sweep in `qa/out/sweep/axe.json` (4 serious, 4 moderate), so inherited and by design, not a finding of this branch. Operator meanwhile committed 9613c46 (the migration plan filed as a closed session record) onto this branch.

---

<!-- kiss-branch-close writes the reflection below and flips status: closed -->

## Reflection (written at kiss-branch-close, 2026-09-12)

### Reflect — what the session was

A dependency upgrade that turned into an adoption exercise: kiss-ssg 2.1.0 → 2.2.1, plus the two 2.2 features this site had a use for (`aliases` and `{{link}}`) and the first knowledge-base record. It was **planned**: the open wrote ten criteria against the changelog and the page-id table before a line changed, and the one genuine unknown — whether `generate.js`'s hand-built URLs could share the `{{link}}` resolver — was named as a criterion with both outcomes acceptable and settled at the first pulse (`kiss.handlebars.helpers.link` is callable at render time; `linkTo()` wraps it). The shape served the work: four slices, each verified by a tree diff of `docs/` before it was committed. One framing error: the open predicted that only pages carrying a slashless section href would change, forgetting the navbar is on every page. The tree diff, not the hash diff, became the proof of losslessness, and the pulse log says so.

### Evaluate — how the human supervised the AI

Three dimensions discriminated this session.

**Pushback & steering** was the strongest. After the open the agent reported "intent captured, branch ready", and the operator asked "I can't see where you've used the new link method". That was exactly right: nothing had been built, and the report could be read as if it had. The question converted a planning artefact into the first slice. Two further questions — whether the site should go `extensionLess` for SEO (answer: no, every URL is already extension-free and a switch would 301 every inner page for nothing), and "any other 2.2.1 improvements we can leverage?" — were **learning engagement** of the right kind: conceptual, ahead of code, and each produced a decision rather than more code.

**Verification & ownership** was mixed. The operator ran the plugin commands themselves and asked for verification of each, and accepted the agent's before/after tree diffs as evidence for the byte-identical criterion. But the two "eyeball" criteria — the rendered navbar and breadcrumb after the `{{link}}` swap, and the deploy preview — were not looked at by a human; the first was covered by a mechanical diff, the second is pending a PR. All three pulses were run by the agent on its own cadence; the operator did not ask for one. The cadence held anyway, which is the point of the loop, but the intent to supervise sat with the skill rather than the person.

**Iteration discipline** was good: slices of 65, 14 and 5 changes, each with a build, a diff and a commit, and a check that stayed green throughout. One coordination hazard: the operator edited and staged files in the same working tree mid-branch, and the CLAUDE.md commit swept in a staged rename and an intro edit that were theirs. Intended, harmless, but not something either party chose.

**Harness leverage**: the kiss-memory loop was used end to end, AskUserQuestion settled scope with one question, the slow QA run went to the background. The operator kept the plugin toolchain itself current (marketplace update, kiss-memory install) before the work started.

Level: **Active supervisor.** Framed the work through the open, asked conceptual questions before accepting direction, caught the one moment the report outran the work, and verified through the agent's evidence. Not yet **agentic engineering lead**: no independent review pass, eyeball criteria delegated to diffs, and the shared working tree mid-branch.

### Feedback — recommendations for next time

- **Human — refresh `qa/baseline/content.json` in the same PR that changes copy or URLs.** PR #24 renamed `/find-us/` and added breadcrumbs, the ladder and forms without refreshing it, so the compare gate failed on master before this branch opened and was noise here. Next time: `npm run qa:baseline` is part of any content PR's checklist, not a master-side chore.
- **Human — do not stage or edit in the working tree the agent is committing from, or say so.** The CLAUDE.md commit carried two of your changes by accident. Next time: a worktree for parallel edits, or a one-line "I've staged X" before the agent's next commit.
- **Human — look at one rendered page per slice when a criterion says eyeball.** The navbar swap was proven by diff, but the preview criterion is still open. Next time: open the deploy preview and click the three section links before calling close.
- **Agent — when a criterion predicts which pages change, read the partial index in `AIKB/site-map.md` first.** The navbar is rendered by all 19 pages; the open's "4 occurrences" estimate counted template lines, not pages. Next time: state the predicted diff in pages, derived from the partial → pages table.
- **Agent — say "no site code has changed yet" when the open finishes.** The operator had to ask. Next time: the open's report ends with what has and has not been built.
- **Agent — backtick only full repository paths in notes.** The first draft of the notes produced four `dangling` findings from bare filenames and a bare extension. Next time: `src/models/about/gaynor-probert.json`, never the bare filename.
- **Agent — write multi-file prose with the file tool, not shell heredocs.** Two long heredoc commands failed on quoting in this session and each cost a turn — the second while writing this very reflection. Next time: Write per file, Bash only for the check and the commit.

### Verdict — did we achieve the objective?

**Brief:** move the site from kiss-ssg 2.1.0 to 2.2.1 and take up `aliases` and `{{link}}`, with the build output otherwise byte-identical.

- [x] `^2.2.1` in `package.json` and the lockfile; check `ok`, 19 pages, 0 failed, 513 internal references, none broken (was 570: three section links × 19 pages deduplicated once navbar and footer agreed on the slashed form).
- [x] `src/assets/_redirects` deleted; `aliases` on 7 course, 2 consultation and 4 about records, `/find-us/` on the contact `.page()`.
- [x] Generated `docs/_redirects` carries the same 14 from → to pairs (sorted diff against the old file); `report().redirects` = `aliases: 14, collisions: [], removed: [], moved: []`; assets 92 → 91.
- [x] 65 hrefs and one form action across 12 templates and partials go through `{{link "<id>" canonical=true}}`; no hardcoded internal page path remains in `src/pages`, `src/partials`, `src/layouts` — nor in `src/models` (the about records' `next.link` became `next.id`).
- [x] `canonical=true` on every link; tree diff of `docs/` before and after is identical once the navbar's `/courses`, `/about`, `/behavioural-consultations` are normalised to the slashed form. Correction to the criterion's own estimate: every page is `~`, not four, because the navbar is on all of them; the diff is the proof, and the slash removes a Netlify 301 on each click.
- [x] `generate.js` shares the resolver: `linkTo()` over `kiss.handlebars.helpers.link`, used by `SECTIONS`, the breadcrumb and the course ladder at render time.
- [x] Five notes written and stamped (`about`, `behavioural-consultations`, `course`, `faqmapper`, `pipeline/tailwind`); the check prints no `note …:` line.
- [x] CLAUDE.md: Deploy names `^2.2.1` and the three 2.2 features; the `_redirects` paragraph rewritten; a `{{link}}` rule and a Knowledge-base section added.
- [x] `.claude/settings.json` enables `kiss-ssg@kiss-ssg` and `kiss-memory@kiss-ssg`.
- [ ] Eyeball: `npm run qa` — page count, no-Bootstrap, SEO, snapshot pass; **compare fails, inherited** (baseline from PR #23, fails on master identically); axe: 8 violations, all on the Google verification stub, identical to the 2026-09-09 sweep, real pages clean. `/pr-verify` not yet run — no PR exists.

**Met.** Concretely better: an internal link to a page that does not exist now fails the build instead of shipping; the 14 legacy redirects are generated from the pages they point at and audited every build for removals, moves and collisions; the navbar no longer sends every section click through a 301; the site has a recorded map and a stamped note for each thing that carries judgement. Open: refresh the QA baseline on master (not this branch's); run `/pr-verify` once a PR is opened; four follow-ups identified and deferred — a deploy gate on `report().links.broken` and `redirects.collisions`, a `check` job on pull requests, `// @ts-check` on `generate.js`, and an authored `AIKB/site.md`.

