{{#extend "layout"}}

{{#content "main"}}
  <div class="mx-auto max-w-2xl py-8 text-center sm:py-12">
    <p class="mb-3 font-display text-5xl text-brand-700 sm:text-6xl">404</p>

    <div class="page-header">
      <h1>We can&rsquo;t find that page</h1>
    </div>

    <p class="mb-6 text-lg">
      The page may have moved, or the link that brought you here may be out of date.
      Nothing is wrong with your dog.
    </p>

    <div class="flex flex-wrap justify-center gap-3">
      <a href="{{link "courses/index" canonical=true}}" class="btn btn-primary">Dog training courses</a>
      <a href="{{link "behavioural-consultations/index" canonical=true}}" class="btn btn-secondary">Behavioural consultations</a>
    </div>
  </div>

  <div class="mx-auto mt-12 max-w-3xl">
    <h2 class="mb-4 text-center font-display text-xl text-ink-900">Or try one of these</h2>
    <ul class="grid gap-3 sm:grid-cols-2">
      <li class="card p-4">
        <a href="{{link "index" canonical=true}}" class="font-medium text-brand-700">Home</a>
        <p class="mt-1 mb-0 text-sm text-ink-600">Dog behaviour and training in South Wales.</p>
      </li>
      <li class="card p-4">
        <a href="{{link "about/index" canonical=true}}" class="font-medium text-brand-700">About Gaynor</a>
        <p class="mt-1 mb-0 text-sm text-ink-600">Her approach, the team and the training grounds.</p>
      </li>
      <li class="card p-4">
        <a href="{{link "contact" canonical=true}}" class="font-medium text-brand-700">Get in touch</a>
        <p class="mt-1 mb-0 text-sm text-ink-600">Send a message, and find both training venues on the map.</p>
      </li>
      <li class="card p-4">
        <a href="tel:+447798500390" class="font-medium text-brand-700">07798 500390</a>
        <p class="mt-1 mb-0 text-sm text-ink-600">Call Gaynor if you would rather talk it through.</p>
      </li>
    </ul>
  </div>
{{/content}}

{{/extend}}
